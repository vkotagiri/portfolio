version: "3.9"
services:
  api:
    build: .
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:///./portfolio.db}
      - PORT=8000
      - TIMEZONE=America/New_York
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
